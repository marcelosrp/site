import styled from 'styled-components'

export const About = styled.section`
  margin: 5rem 0;
  width: 80rem;

  p {
    font-size: 5rem;
    font-weight: var(--extraBold);
    line-height: 1.7;
  }

  button {
    margin-top: 2.5rem;
  }
`
